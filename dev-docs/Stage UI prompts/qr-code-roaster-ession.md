3. Kiedy `roaster` wprowadzi dane wymagane do stworzenia `roaster-coffee-tag` i najedzie na przycisk `btn-generate-qr` wysyłający dane do Supabase, przycisk musi zmienić stan onhover. Po kliknięciu przycisku dane zostają wysłane do Supabase, tabela roaster_coffee_tags (tak jest obecnie), na stronie formularza wyświetlony zostaje komunikat "Success! Coffee Tag has been saved." i uruchomiona musi zostać sesja tworzenia kodu QR na serwerze **funcup** (self-hosted QR code solution). 
Sesja tworzenia kodu QR uruchomiona zostaje  Napis na  `btn-generate-qr` przekształca uzupełnione informacje `roaster-coffee-tag` w kod QR w formacie .svg do pobrania.
